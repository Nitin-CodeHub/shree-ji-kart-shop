// This component has been replaced by AuthDialog.tsx
// Keeping this file for backward compatibility but it's no longer used

import React from 'react';

const LoginDialog = () => {
  console.warn('LoginDialog is deprecated. Use AuthDialog instead.');
  return null;
};

export default LoginDialog;
